package main

// Version is the version of the application. It is set at compile time using the -ldflags flag.
var Version = "development"

// BuildDate is the date the application was built. It is set at compile time using the -ldflags flag.
var BuildDate = "-"
